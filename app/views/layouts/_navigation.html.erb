<nav class="navbar navbar-inverse">
  <div class="container-fluid"></div>
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "ExpenseIt", root_path, class: "navbar-brand", id:"logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to "My Expenses", user_expenses_path(current_user.id) %></li>
          <li><%= link_to "Add New Expense", new_user_expense_path(current_user.id) %></li>
          <li><%= link_to 'Expense Report' , report_user_path(current_user)%></li>
          <li><%= link_to("Logout", destroy_user_session_path, :method => :delete) %></li>
          <% if current_user.admin? %>
            <li><%= link_to 'All Users', users_path(current_user)%></li>
          <%end%>
        <% else %>
          <li><%= link_to 'Login', new_user_session_path%></li>
          <li><%= link_to 'Sign-Up', new_user_registration_path%></li>  
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

